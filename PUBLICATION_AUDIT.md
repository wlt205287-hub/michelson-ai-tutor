# 公开前审核记录

日期：2026-09-14。范围为独立仓库的候选提交文件、忽略规则、常见敏感内容模式、素材标注和当前服务监听状态。不是完整代码安全审计、版权鉴定或法律意见。第 2 至 5 节保留初次审核及素材确认记录；后续 Git 暂存与提交前验证见第 6 节，未修改业务代码。

## 1. 当前结论

**技术文件整理检查通过；作者已确认全部教学素材为本人原创，人名来源文件名已中性化。公开署名为 Seasoner，作者选择暂不添加许可证。已初始化独立 Git 仓库并暂存 86 个文件，尚未上传成功。**

- 作者已在本次审核中确认：团队、学校或竞赛方面允许 Agent 后端单独公开。这里记录作者确认，不代表另行审阅过书面协议。
- 候选提交文件未包含真实 `.env`、运行输出、虚拟环境或索引；必要的标准问答 JSON、manifest、图片与展示材料均在候选列表中。
- 未发现本次所检查的疑似真实密钥、私钥、邮箱或手机号模式。模式扫描无法证明所有形式的秘密均不存在。
- 作者在后续明确确认：四张教学图片、基础知识库、标准问答和全部 RAG 文档均为本人原创，不只是对他人材料的整理或改写。本记录以作者最新确认作为素材声明，不代表独立版权鉴定。
- 按作者要求，一处 RAG 来源字段已改为不含人名的 `主报告模板.docx`，保留实验方案及报告模板的来源关系；知识正文和 mask 要求未修改。
- 作者指定公开署名 Seasoner，并明确选择暂不添加许可证。未创建 LICENSE，未将允许修改、再分发和商用的意向写成正式 MIT 或其他许可声明。

## 2. 候选文件与忽略规则

审核起点共 85 个候选文件，总大小 5,404,779 字节。最大文件为 `assets/images/fringe_in_out.gif`，3,784,907 字节。该统计不含本次新增审核记录与随后更新的文档字节数。

候选清单通过独立目录的 `.gitignore` 和只读 Git 枚举获得；使用不存在的临时索引避免原团队仓库同名已跟踪文件影响枚举，未写入原索引或创建独立 `.git`。

| 检查项 | 结果 |
| --- | --- |
| 根目录 `.env`、`agent_demo/.env` | 被忽略；未读取内容 |
| `outputs/`、`.venv/`、`rag_index/` | 被忽略，不作为上传材料 |
| `.env.example` | 保留，属于公开配置模板 |
| `assets/standard_qa.json`、`assets/image_manifest.json` | 未漏交 |
| 3 张 JPG 和 1 张 GIF | 未漏交；作者后续确认均为本人原创 |
| `showcase/` 的合成输入与真实输出 | 未被忽略，来源与局限见演示总览 |
| 57 个初次提取文件 | 初次审核时哈希全部一致；后续仅 1 处 RAG 来源字段中性化，见 VERIFICATION 第 10 节 |
| Git 历史 | 独立目录无 `.git`，没有旧历史随包带入 |

不要直接把整个本地目录压缩上传：`.gitignore` 只约束 Git 文件选择，不会自动从普通压缩包中排除 `.env` 和运行数据。将来执行 `git add` 后仍需重新检查实际暂存区，本次没有可供审核的暂存快照。

## 3. 敏感信息检查

对候选文本检查了常见长格式 API/GitHub/AWS token、PEM 私钥头、Bearer 串、明显的硬编码 secret 字面量、邮箱、手机号和 Windows 绝对路径。输出仅包含命中位置和规则，不输出疑似秘密值。

唯一自动命中位于 `test_cases/test_agent_cli_behavior.py:238`，是验证 CLI 不泄漏本地路径的虚构断言，不是用户真实路径或密钥，不需要删除测试。

人工核对来源标注时，发现 `rag_docs/grading/2026_表面形貌实验报告检查量规.md:6` 的 `source_file` 中含带人名的原始文档文件名。本记录不再次复制该名字。作者确认全部教学文本为本人原创，并要求该文件名不含人名，现已改为 `实验方案 - 副本.md; 主报告模板.docx`。这是开发资料的中性来源标签，不是待创建的本地文件；公开作者署名按其要求使用 Seasoner。

## 4. 教学素材与来源

| 材料 | 已观察到的情况 | 作者最新确认 |
| --- | --- | --- |
| `assets/images/michelson_optical_path.jpg` | 光路图；JPEG，无 EXIF 标签 | 本人原创 |
| `assets/images/compensation_plate.jpg` | 补偿板图；JPEG，无 EXIF 标签 | 同上 |
| `assets/images/equal_inclination_rings.jpg` | 等倾原理图；JPEG，无 EXIF 标签 | 同上 |
| `assets/images/fringe_in_out.gif` | 动态教学图；GIF，无 EXIF 标签 | 同上 |
| `docs/04_实验知识库初版.md` | 基础运行知识库 | 本人原创 |
| `assets/standard_qa.json` | 5 组标准问答 | 本人原创 |
| `rag_docs/` 的 5 篇资料 | frontmatter 保留源资料标签，报告模板标签已去除人名 | 本人原创，不只是整理或改写第三方材料 |

图片检查仅确认格式与元数据标签；没有 EXIF 不等于版权证明。原创状态来自作者声明，不是从图片格式或来源字段推断；作者当前选择不添加许可证，因此本记录不另行制定公开使用条款。

## 5. 服务与展示边界

- 审核时端口 8000 监听地址为 `127.0.0.1`，没有观察到该端口绑定 `0.0.0.0` 或公网地址。这不包含反向代理、端口转发或系统级网络暴露审计。
- 后端仍缺少鉴权、用户隔离与完整限流，不能因本机启动成功就作为公网服务部署。
- README 与 showcase 明确保留 mask 遗留要求和 q3 重复干扰项。本次不修复这两个问题，也不将成功响应当作内容准确率证据。
- 三个展示输入是合成材料，不是真实学生作业；原始运行目录仍被忽略。
- 初次审核未调用模型或执行全量业务测试；后续来源字段中性化的变更和验证另见 VERIFICATION 第 10 节。

## 6. 首次提交准备

1. 已初始化独立仓库并检查实际暂存文件，共 86 个文件；未携带原团队 Git 历史。真实配置、运行输出、虚拟环境和索引不在暂存区。
2. 按作者选择暂不添加 LICENSE，不在仓库描述中宣称采用 MIT 或其他开源许可证。
3. 作者已提供 GitHub 远程地址和 noreply 提交邮箱，并授权首次提交与上传。不更改仓库可见性、不强制覆盖远程。
4. 本轮提交前重新运行 194 项离线测试，全部通过。GitHub 远程检查遇到 `Could not resolve host: github.com`，尚未确认远程内容或上传结果；后续以实际 Git 验证为准。
